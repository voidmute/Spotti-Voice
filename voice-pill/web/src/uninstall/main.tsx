import { createRoot } from "react-dom/client";
import { UninstallApp } from "./UninstallApp";
import "../settings/settings.css";

createRoot(document.getElementById("root")!).render(<UninstallApp />);
